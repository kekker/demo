// Netlify context variables are accessible only during build time
// To access them in runtime, you need to inject it directrly
// This is a placeholder to inject a netlify CONTEXT variable
// use with build configuration:
// sed -i s/CONTEXT_PLACEHOLDER/${CONTEXT}/g src/utils/inject-context.js && gatsby build
module.exports = "CONTEXT_PLACEHOLDER";
